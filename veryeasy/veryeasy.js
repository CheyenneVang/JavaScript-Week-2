function min(numOne, numTwo) {
    // Initially created an if/else statement
    // if(numOne < numTwo) {
    //   return numOne;
    // } else (numOne > numTwo)
    //   return numTwo;
    return Math.min(numOne, numTwo);
  }
  console.log(min(5, 10)); // outputs 5
  console.log(min(20, 10)); // outputs 10

// Math.min() is a function that returns the smallest number of the two numbers that are put in the parameter